services:
  dentist-prod:
    build:
      context: .
      dockerfile: Dockerfile
      target: prod
    container_name: dentist-portfolio-prod
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
